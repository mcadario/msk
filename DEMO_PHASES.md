Phase 1 — ls and cat README.md run, then make test-integration succeeds on v1 repo. Evaluator finds it via test_keywords. Task marked success. Memory updater strengthens the seeded make K-node. Formation creates a new tool_pattern K-node for make test-integration.
Phase 2 — Same. make K-node reactivated, strengthened again. Strength climbing toward 0.90+.
Phase 3 — Repo is now v2. make K-node reactivated and injected into plan. Executor runs make test-integration — fails (rc=2). Executor continues to npm run test:integration — succeeds. Evaluator finds npm success. Memory updater calls record_use(success=False) on the make K-node — strength drops by 0.15. Formation sees the npm success event and creates a new tool_pattern K-node for npm run test:integration. Formation also sees the make failure and creates an episode K-node for the failure.
Phase 4 — npm K-node now exists and gets reactivated. It appears in agent_guidance as a preferred command, so _fallback_plan puts it first. Executor runs npm directly and succeeds. Make K-node either not reactivated or if reactivated gets record_use(success=True) — but since npm ran and succeeded, make never executed so it shouldn't be strengthened.